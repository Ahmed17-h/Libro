@model IEnumerable<Libro.Models.Loan>

@{
    ViewData["Title"] = "Unpaid Fines";
}

<span class="page-kicker">Collections</span>
<h2>Unpaid Fines</h2>

<div class="loan-list">
    @foreach (var loan in Model)
    {
        <div class="loan-card">
            @if (!string.IsNullOrWhiteSpace(loan.Book?.ImageUrl))
            {
                <img src="@loan.Book.ImageUrl" alt="@loan.Book.Title" class="loan-card-cover" />
            }
            else
            {
                <div class="loan-card-cover-placeholder">📖</div>
            }

            <div class="loan-card-body">
                <div class="loan-card-title">@loan.Book?.Title</div>
                <div class="loan-card-meta">
                    <span>Member: @loan.Member?.FullName</span>
                    <span class="stamp stamp-overdue">@loan.Fine?.ToString("C")</span>
                </div>
                <div class="loan-card-actions">
                    <form asp-action="MarkFinePaid" method="post">
                        <input type="hidden" name="loanId" value="@loan.Id" />
                        <button type="submit" class="btn btn-sm btn-success">Mark as Paid</button>
                    </form>
                </div>
            </div>
        </div>
    }
</div>